[package]
name = "rhazel"
version = "0.2.0"
edition = "2021"
description = "AArch64 assembler for ruzu's rdynarmic arm64 backend (oaknut's counterpart)"
license = "GPL-2.0-or-later"
repository = "https://github.com/vricosti/rhazel"

[lib]
name = "rhazel"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
